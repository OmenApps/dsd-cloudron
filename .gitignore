# Specs and plans are kept locally and never committed (project convention).
docs/superpowers/specs/
docs/superpowers/plans/

# Local virtual environment and Python build artifacts.
.venv/
*.egg-info/
__pycache__/
.pytest_cache/
build/
dist/
docs/_build/
example-cloudron-apps/
example-dsd-plugins/

# Coverage data and reports (pytest --cov / coverage run).
.coverage
.coverage.*
htmlcov/
coverage.xml

# Captured HTTP archives (e.g. SSO debugging) can carry tokens and cookies.
*.har
