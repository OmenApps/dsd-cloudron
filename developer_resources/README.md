# Developer resources

Captured, redacted `cloudron` CLI outputs used to develop and reason about the
plugin offline. Regenerate against a real server when the CLI changes. Never
commit unredacted tokens, passwords, or addon credentials.
