{{current_settings}}

# dsd-cloudron settings.
# Platform configuration lives in cloudron_settings.py beside this file; it is
# inert unless CLOUDRON_APP_ORIGIN is set, so local development is unaffected.
from .cloudron_settings import *
