# {{ cookiecutter.project_name }}

Scaffolded by dsd-cloudron. See README-cloudron.md for the deploy control surface.

```bash
uv sync
uv run python manage.py migrate
uv run python manage.py runserver
```
